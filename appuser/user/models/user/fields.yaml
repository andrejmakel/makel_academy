# ===================================
#  Form Field Definitions
# ===================================

fields:
    id:
        label: ID
        disabled: false

    username:
        label: username
        disabled: false

    password:
        label: password
        type: password
